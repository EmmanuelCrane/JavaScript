let alturaCm = 172;
let alturaMt = 1.72;
let peso = 68.800;
let alturaRedondeadaHaciaArriva = Math.floor(alturaMt);
let pesoRedondeadaHaciaAbajo = Math.ceil(peso);
let maximoValor = Number.MAX_VALUE + 1 == Number.MAX_VALUE ? `${Number.MAX_VALUE + 1} Es el valor maximo que se puede obtener en JavaScript` : `${Number.MAX_VALUE + 1} No es el maximo valor que se pede obtener con JavaScript`;